#!/usr/bin/env python3

from git import Repo

class ConfPool :
    def __init__( self,
                  path,
                  operation_url,
                  base_url ) -> None :

    def get_cods( self ) -> list :

    def get_confs( self,
                   regex ) -> list :

    def propagate_cod( self,
                       base_ref,
                       key_regex ) -> list :

    def tag( self,
             base_ref,
             key_regex ) -> list :

    
